# YouTube Tutorial: Login Page #
Teacher: (http://www.#.com)  
Login pages help legitimize sites that act as business conduits. Separating a site's inner content from basic advertising draws (w/ other previews) that are found on main landing pages offer subscibers experiences based on exclusivity.

### Technologies ###
In this demonstration //the teacher// uses the following:
- .ejs files are reusable components that can be used with variables
- passport (https://www.passportjs.org/) is authentication middleware

### Goals ###
1. simply complete the tutorial as demonstrated above
2. use CSS techniques to spruce up the working result
3. (stretch) create a login page that determines route based on user roles:
    - administrator
    - expert roles
    - user roles